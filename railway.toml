[build]
buildCommand = "cd web && npm ci && npm run build && cd .. && python scripts/cache_model.py"

[deploy]
startCommand = "cd server && python main.py"
healthcheckPath = "/api/v1/health"
healthcheckTimeout = 120
