[build]
buildCommand = "cd web && npm ci && npm run build && U2NET_HOME=/app/.u2net python -c \"from rembg import remove; print('rembg model cached')\""

[deploy]
startCommand = "cd server && python main.py"
healthcheckPath = "/api/v1/health"
healthcheckTimeout = 120
